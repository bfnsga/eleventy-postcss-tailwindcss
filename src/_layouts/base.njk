<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "head.njk" %}
      <!-- capture the CSS content as a Liquid variable -->
      {% set css %}{% include "app.compiled.css" %}{% endset %}
      <style> 
        {{ css | safe }}
      </style>
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>